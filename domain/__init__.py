"""Domain data models and routing constants.

Pure Pydantic models and lookup tables shared across the orchestration layer.
This package is a leaf — it does not depend on any other in-project package.
"""
